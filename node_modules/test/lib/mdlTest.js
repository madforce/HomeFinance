var events = require('events');
var util = require('util');

function test() {
    this.TestEvent = function(data) {
        this.emit('data', data);
    }
    this.version = "0.0.0";
}

util.inherits(test, events.EventEmitter);

module.exports = new test();